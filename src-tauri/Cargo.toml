[package]
name = "essence-desktop"
version = "0.3.0-alpha.0"
description = "Essence internal desktop preview"
authors = ["Essence"]
edition = "2021"
license = "UNLICENSED"
publish = false

[lib]
name = "essence_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
